name: Deploy Infrastructure (AWS CDK)

# Prerequisite this workflow can't do for itself: `cdk bootstrap` must be run once, manually, by
# someone with real (not this workflow's scoped-down) AWS credentials — it provisions the
# cdk-hnb659fds-* roles that InfraDeployRole is only ever allowed to assume. See
# docs/deployment-guide.md. Everything after that one-time step is this workflow's job.
on:
  push:
    branches: [main]
    paths:
      - "infrastructure/**"
      - ".github/workflows/infra.yml"
  pull_request:
    branches: [main]
    paths:
      - "infrastructure/**"
      - ".github/workflows/infra.yml"

concurrency:
  group: infra-deploy
  cancel-in-progress: false

permissions:
  id-token: write # required for AWS OIDC federation
  contents: read

env:
  AWS_REGION: ap-south-1

jobs:
  # No `environment:` here on purpose - this job only ever synths/diffs, and gating a read-only
  # `cdk diff` behind a manual approval would make every PR wait on a human for zero benefit.
  # Deploying to the account is exactly what `deploy` below exists to gate.
  synth:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infrastructure

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: mvn clean verify
        run: mvn -B clean verify

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install AWS CDK CLI
        run: npm install --global aws-cdk

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_INFRA_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # InfraDeployRole carries no direct permissions of its own - it can only assume the CDK
      # bootstrap roles (see CicdStack). The CDK CLI does that assumption automatically once
      # `cdk bootstrap` has been run for this account/region, using the role ARNs embedded in
      # each stack's own synthesized template.
      - name: cdk synth
        run: cdk synth --all

      - name: cdk diff
        if: github.event_name == 'pull_request'
        run: cdk diff --all

  # Infra changes (IAM, security groups, KMS, deletion policies) carry a bigger blast radius than
  # an app-server JAR swap, which is why this - unlike synth above - runs under the `production`
  # GitHub environment: whatever protection rules (required reviewers, wait timer) are configured
  # there apply here, the same as backend.yml's deploy job.
  deploy:
    needs: synth
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        working-directory: infrastructure

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install AWS CDK CLI
        run: npm install --global aws-cdk

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_INFRA_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: cdk deploy
        run: cdk deploy --all --require-approval never --concurrency 4
